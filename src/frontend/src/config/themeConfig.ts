interface componentsConfigInterface {
  paperElevation: number;
  borderRadius: string;
  notificationFontSize: number;
}

const componentsConfig: componentsConfigInterface = {
  paperElevation: 16,
  borderRadius: "20px",
  notificationFontSize: 15,
};

export default componentsConfig;
